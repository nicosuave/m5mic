[package]
name = "m5mic-ios-core"
version = "0.1.0"
edition.workspace = true
license.workspace = true

[lib]
crate-type = ["staticlib", "rlib"]

[dependencies]
m5mic-protocol = { path = "../protocol" }
