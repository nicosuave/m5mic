[workspace]
members = [
    "protocol",
    "receiver",
    "firmware",
    "virtual-mic",
    "statusbar",
    "coreaudio-driver",
    "ios-core",
]
default-members = ["protocol", "receiver"]
resolver = "2"

[workspace.package]
edition = "2021"
license = "MIT"
